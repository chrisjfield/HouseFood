import { styleState } from '../interfaces/styleInterfaces';

const styles: styleState = {
    snackbarBody: {
        textAlign: 'center',
    },
    chip: {
        display: 'inline-flex',
        marginRight: '4px',
    },
};

export default styles;
